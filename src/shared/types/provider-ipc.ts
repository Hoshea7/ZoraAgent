export const PROVIDER_IPC = {
  GET_REFERENCE_IMPACT: "provider:get-reference-impact",
  TEST_MODELS: "provider:test-models",
  FETCH_MODELS: "provider:fetch-models",
  CANCEL_TEST: "provider:cancel-test",
} as const;
